🎯 **What:** The `update` function in `Linux/overlay.py` was missing test coverage. The `test_overlay.py` file has been enhanced to include tests for both `read_text` (success and failure cases) and `update` using the `unittest.mock` framework.

📊 **Coverage:** The tests now verify that `read_text` returns the correct string when the file exists, returns 'keine Datei' when a `FileNotFoundError` occurs, and that `update` properly configures the UI label and schedules recursive calls using `root.after`. It mocks the `tkinter.Tk` variables `root` and `label` in `Linux/overlay.py`.

✨ **Result:** Test coverage for `Linux/overlay.py` has improved. The tests confirm they catch bugs when the file is intentionally broken, leading to more resilient GUI components.
