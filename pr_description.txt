🎯 **What:** Replaced the generic `except Exception:` clause with the specific `except FileNotFoundError:` in `Linux/overlay.py`, `Raspi/overlay.py`, and `Windows/overlay.py`. Also updated the corresponding unit tests (`test_overlay.py` in each directory) to mock `FileNotFoundError` instead of `Exception` or `IOError`.

💡 **Why:** Catching generic exceptions (even `Exception`) can accidentally mask unrelated programmatic errors, such as syntax or type errors. Since the only expected failure mode here is a missing file when attempting to read the configuration, `FileNotFoundError` is the precise and correct exception to catch. This improves debuggability and aligns with Python best practices for file I/O operations.

✅ **Verification:**
- Successfully ran `python3 -m py_compile` on all modified files to ensure syntax integrity.
- Verified that all unit tests (`python3 -m unittest discover -s [Platform] -p 'test_*.py'`) for all three platform directories pass successfully after the update.
- Used `grep` to confirm that no other `except Exception:` clauses exist in the Python files within the repository.

✨ **Result:** A safer, more maintainable code structure that avoids swallowing unexpected errors during file I/O, maintaining full functional parity with better error hygiene.
