1. Add cross-distribution package manager support to `shared_build.sh`.
   - Update `install_dependencies()` to detect `dnf`, `zypper`, `pacman`, or `apt` and install the required tools accordingly.
2. Complete pre commit steps
   - Ensure proper testing, verify syntax, and review changes.
